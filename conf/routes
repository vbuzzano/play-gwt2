# This file defines all module routes (Higher priority routes first)
#
# import these routes in the main app as :
# *     /                       module:play-gwt2
#
# ~~~~
%{
 ppath = play.Play.configuration.getProperty("gwt2.publicpath", "gwt-public")
}%

*    /${ppath}/{module}/{service}	GWT2Controller.invoke